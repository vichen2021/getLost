import { Steps as Base } from '@arco-design/mobile-react';
import { StepsProps as Props } from '@arco-design/mobile-react/esm/steps';
import '@arco-design/mobile-react/esm/steps/style/css/index.css';

/**
 * 步骤条
 * 显示一个任务的进度; 或者引导用户完成某个复杂任务。
 */
export default function Steps(props: Props) {
	return <Base {...props} />;
}
