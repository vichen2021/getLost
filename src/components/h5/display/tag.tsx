import { Tag as Base } from '@arco-design/mobile-react';
import { TagProps as Props } from '@arco-design/mobile-react/esm/tag';
import '@arco-design/mobile-react/esm/tag/style/css/index.css';

/**
 * 标签
 * 标签组件，支持标签和标签组。
 */
export default function Tag(props: Props) {
	return <Base {...props} />;
}
