import { Rate as Base } from '@arco-design/mobile-react';
import { RateProps as Props } from '@arco-design/mobile-react/esm/rate';
import '@arco-design/mobile-react/esm/rate/style/css/index.css';

/**
 * 评分组件
 * 评分组件，支持受控模式
 */
export default function Rate(props: Props) {
	return <Base {...props} />;
}
