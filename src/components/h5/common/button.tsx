import { Button as Base } from '@arco-design/mobile-react';
import { ButtonProps as Props } from '@arco-design/mobile-react/esm/button';
import '@arco-design/mobile-react/esm/button/style/css/index.css';

/**
 * 按钮
 * 按钮用于开始一个即时操作。
 */
export default function Button(props: Props) {
	return <Base {...props} />;
}
